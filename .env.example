# Copy this file to .env for LOCAL testing only (and keep .env out of git -
# it already is, via .gitignore). In GitHub Actions, this value comes from
# the repository secret SERPAPI_API_KEY instead - see README "Secrets".
SERPAPI_API_KEY=your_serpapi_key_here
